# Электронные перевозочные документы — eCMR и ЭТрН

## Обзор

Электронные перевозочные документы (ЭПД) заменяют бумажные транспортные накладные в юридически значимом электронном документообороте. Система поддерживает два стандарта:

- **eCMR** — электронная международная автомобильная накладная по Дополнительному протоколу к Конвенции КДПГ (CMR), вступившему в силу в 2008 году.
- **ЭТрН** — электронная транспортная накладная в соответствии с законодательством РФ (Постановление Правительства РФ № 2116), передаётся через государственную информационную систему электронных перевозочных документов (ГИС ЭПД).

## Форматы и стандарты

### eCMR (UN/CEFACT)

- Формат обмена данными: **XML** на основе стандарта UN/CEFACT (Universal Business Language, UBL).
- Семантическая модель: `TransportationStatus`, `ConsignmentItem`, `PartyIdentification`.
- Передача: через операторов сети eCMR (IRU SmartCMR, TIMOCOM и др.) или напрямую через API партнёров.

### ЭТрН (ФНС / Минтранс РФ)

- Формат: **XML** по приказу ФНС России об утверждённых форматах перевозочных документов.
- Передача: через аккредитованных операторов ИС ЭПД (Контур, СБИС, Такском).
- Регистрация в ГИС ЭПД: каждый ЭТрН получает уникальный идентификатор в государственной системе.

## Жизненный цикл титулов ЭТрН

ЭТрН состоит из четырёх последовательно подписываемых титулов:

```
Титул 1 (Грузоотправитель)
  → Сведения о грузе, маршруте, перевозчике
  → Подписание УКЭП грузоотправителя
        ↓
Титул 2 (Перевозчик — приёмка)
  → Фиксация факта приёмки груза водителем
  → Подписание УКЭП/ПЭП водителя или перевозчика
        ↓
Титул 3 (Грузополучатель)
  → Фиксация факта получения груза
  → Указание расхождений (если есть)
  → Подписание УКЭП грузополучателя
        ↓
Титул 4 (Перевозчик — сдача)
  → Финальная отметка перевозчика о сдаче груза
  → Подписание УКЭП перевозчика → Закрытие ЭТрН
```

### Статусы ЭТрН в системе

| Статус           | Описание                                         |
| ---------------- | ------------------------------------------------ |
| `DRAFT`          | Черновик, не передан оператору                   |
| `TITLE_1_SIGNED` | Подписан Титул 1 грузоотправителем               |
| `TITLE_2_SIGNED` | Подписан Титул 2 перевозчиком (груз принят)      |
| `TITLE_3_SIGNED` | Подписан Титул 3 грузополучателем (груз получен) |
| `COMPLETED`      | Подписан Титул 4, ЭТрН закрыт                    |
| `DISPUTED`       | Зафиксированы расхождения в Титуле 3             |
| `CANCELLED`      | Аннулирован по соглашению сторон                 |

## Взаимодействие с операторами ИС ЭПД

Система интегрируется с аккредитованными операторами посредством REST API:

| Оператор            | Метод интеграции | Особенности                                |
| ------------------- | ---------------- | ------------------------------------------ |
| **Контур (Диадок)** | REST API v2      | Поддержка роуминга с другими операторами   |
| **СБИС (Тензор)**   | REST API + SOAP  | Интеграция с 1С                            |
| **Такском**         | REST API         | Поддержка eCMR для международных перевозок |

### Схема передачи данных

```
Система (CargoFlow)
    │
    ├──[REST API]──→ Оператор ИС ЭПД
    │                    │
    │                    ├──→ ГИС ЭПД (Минтранс)
    │                    │     └── Регистрация, присвоение ID
    │                    │
    │                    └──→ Роуминг с другими операторами
    │
    └──[Webhook]──← Статусы и квитанции от оператора
```

## Хранение ЭПД

- XML-файлы титулов хранятся в S3 как неизменяемые объекты (WORM).
- В PostgreSQL хранятся метаданные и текущий статус ЭТрН/eCMR.
- Квитанции операторов (подтверждения регистрации в ГИС ЭПД) сохраняются как отдельные вложения к документу.
