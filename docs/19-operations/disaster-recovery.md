# План аварийного восстановления (Disaster Recovery Plan / DRP)

## Классификация данных
- **Critical** (Orders, IAM, Payments): RPO = 0 (нулевая потеря), RTO < 15 мин.
- **Recoverable** (OpenSearch search index): RPO < 1 час, RTO < 2 часов.
- **Ephemeral** (Redis Cache, Session State): RPO = N/A, RTO < 5 мин.

## Сценарии катастроф

### Сценарий A: Полный отказ основного дата-центра / зоны доступности (AZ Outage)
- **Действия**: Базы данных (PostgreSQL, Kafka) работают в режиме Multi-AZ. При падении основной зоны происходит автоматический failover на реплику в соседней зоне (RTO ~ 2 минуты). Трафик балансировщика перенаправляется.

### Сценарий B: Логическое повреждение данных (Ransomware / ошибочный DROP TABLE)
- **Действия**: Блокировка доступа к скомпрометированным сервисам. Остановка приема новых транзакций. Восстановление PostgreSQL через PITR (Point-In-Time Recovery) с помощью WAL-G до точной секунды перед инцидентом.

### Сценарий C: Массовый сбой DNS / сетевого магистрального провайдера
- **Действия**: Переключение на резервного DNS провайдера (Route 53 -> Cloudflare). 
